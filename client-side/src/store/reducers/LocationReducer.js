
const initialState = {
    locations: [],
}

const LocationReducer = (state = initialState, { type, payload }) => {

    switch (type) {

        default:
            return { ...state }
    }

}

export default LocationReducer